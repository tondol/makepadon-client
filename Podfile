# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'makepadon-client' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'Kingfisher', '~> 5.0'
  pod 'KeychainAccess', '~> 3.2'
  pod 'APIKit', '~> 4.1'
  pod 'RxSwift', '~> 4.5'
  pod 'RxCocoa', '~> 4.5'

  # Pods for makepadon-client

  target 'makepadon-clientTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'makepadon-clientUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
